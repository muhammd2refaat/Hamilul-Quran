# traefik/traefik.yml
# ─────────────────────────────────────────────────────────────────────────────
# Traefik v3 static configuration
# Traefik sits at the edge and routes incoming requests to the correct service:
#
#   elhafazah-academy.com        →  frontend  (Next.js,  port 3000)
#   admin.elhafazah-academy.com  →  admin     (serve SPA, port 4173)
#   api.elhafazah-academy.com    →  backend   (FastAPI,   port 8000)
# ─────────────────────────────────────────────────────────────────────────────

# ─── API / Dashboard ──────────────────────────────────────────────────────────
# Dashboard is off by default. To re-enable, do NOT set insecure:true again —
# instead route it through a Host() router with BasicAuth + TLS (see the
# commented example at the bottom of providers.docker below) and keep the
# :8080 port mapping out of docker-compose.yml.
api:
  dashboard: true
  insecure: false

# ─── Entry Points ─────────────────────────────────────────────────────────────
entryPoints:
  web:
    address: ":80"
    # Redirect all HTTP → HTTPS
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true

  websecure:
    address: ":443"

# ─── Certificate Resolvers (Let's Encrypt) ────────────────────────────────────
certificatesResolvers:
  letsencrypt:
    acme:
      email: admin@elhafazah-academy.com        # ← change to your real email if needed
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web

# ─── Providers ────────────────────────────────────────────────────────────────
providers:
  docker:
    exposedByDefault: false    # Only route containers with traefik.enable=true label
    network: hamilul-net       # Must match the network defined in docker-compose.yml

# ─── Logging ──────────────────────────────────────────────────────────────────
log:
  level: INFO

accessLog: {}
