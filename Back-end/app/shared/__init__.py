"""
shared/__init__.py
==================
Cross-cutting reusable logic shared across all features.

Sub-packages:
    utils/       — General helpers (UUID, datetime, pagination)
    enums/       — Application-wide enumerations
    helpers/     — Domain helpers (planned)
    validators/  — Reusable Pydantic validators (planned)
    decorators/  — @retry, @transactional, @cache (planned)
    mixins/      — Model/schema mixins (planned)
    types/       — Custom type aliases (planned)
    dto/         — Shared data-transfer objects (planned)
"""
