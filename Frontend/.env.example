# Environment variables for the Hamilul-Quran student/teacher Frontend.
# Copy to .env.local for local development (gitignored).

# Backend API base URL — the FastAPI backend's /api/v1 root.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
